#ifndef SOLVER_HPP
# define SOLVER_HPP

# include <iostream>
# include <string>

class Solver
{

	public:

		Solver();
		Solver( Solver const & src );
		~Solver();

		Solver &		operator=( Solver const & rhs );

	private:

};

#endif /* ********************************************************** SOLVER_H */